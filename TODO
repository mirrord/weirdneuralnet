# TODO
 * impl a couple more activation functions [DONE]
 * impl normalization [DONE]
 * impl regularization [DONE]
 * turn into a real package [DONE]
 * document existing code [DONE]
 * impl network compilation [UNTESTED, INCOMPLETE]
 * impl network parameters config file [DONE]
 * impl batch training [DONE]
 * impl unsupervised learning: clustering [DONE]
    * centroid calc
    * edge case calc
    * unsupervised training types [kmeans & agglomerative]
 * implement more dataset usage
 * impl progress bar instead of stdout reporting
 * impl priming [DONE, UNTESTED]
    * priming A: pretrain with blind clusters
    * priming B: pretrain with blind cluster centroids & edge points
    * priming C: pretrain with nested cluster centroids & edge points from expected dataset
 * break up experiments & package into separate modules
 * separate WeirdNet and basic NN
 * impl transfer training
 * impl some experiments
 * clean up CLI
 * implement more initialization types